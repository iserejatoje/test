name: CI

on:
  - push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Git pull
        run: |
          ssh studioatom_ci@studioatom.beget.tech "cd ${{ secrets.PROJECT_FOLDER }} && git pull"